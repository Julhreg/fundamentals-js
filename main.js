/*
    >	Maior que	(a > b)
    <	Menor que	(a < b)
    >=	Maior ou igual a	(a >= b)
    <=	Menor ou igual a	(a <= b)
    ==	Igual a	(a == b)
    !=	Diferente de	(a !== b)
    ===	Idêntico a	(a === b)
    !==	Não idêntico a	(a !== b)
    &&	E/and	(a && b)
    ll	Ou/or	(a ll b)

        -o símbolo de “idêntico a” (===). Ele não só compara os valores dos dois lados da equação, como também verifica se eles são do mesmo tipo.

    -E assim como os operadores “==” e “===”, usamos a mesma lógica para os seus inversos “!=” e “!==”
*/


/*
    if (COMPARAR O numeroUm e a stringUm) {
  console.log('As variáveis numeroUm e stringUm tem o mesmo valor, mas tipos diferentes')
} else {
  console.log('As variáveis numeroUm e stringUm não tem o mesmo valor')
}
*/

/* var, if e const são palavras reservadas do JavaScript. Ou seja, não podemos usá-las para dar nomes (identificar) variáveis, funções ou outros blocos de código que precisem de identificadores.*/

//tipagem dinâmica -> trocando o tipo de dado 
// untyped

let myVar = 567;
myVar = "text";
myVar = true;

//multiparadigma ->  resolver problemas de várias formas -> mais flexivel

/* linguagem interpretada
- linha por linha 
*/

//NODEJS -> console

// https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Reference/Global_Objects/Math#description